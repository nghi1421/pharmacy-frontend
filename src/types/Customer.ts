export type Customer = {
    id: number,
    name: string
    phoneNumber: string
    address: string
    gender: number
}

export type CustomerData = {
    id: number,
    name: string
    phoneNumber: string
    address: string
    gender: any
}